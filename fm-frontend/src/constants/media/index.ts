export enum MediaType {
  IMAGE = 0,
  VIDEO = 1,
  FILE = 2,
  EMBED_LINK = 3,
  OTHER = 4,
}

export enum EMediaThumbnail {
  PHOTO = "Photo",
  PDF = "PDF",
  WORD = "Word",
  CSV = "CSV",
  EXCEL = "Excel",
  UNKNOWN = "Unknown",
  PPT = "PowerPoint",
  VIDEO = "Video",
  AUDIO = "Audio",
  COMPRESSED = "Compressed",
  EMBED = "Embed",
}

export enum EListMediaType {
  ALL = "All types",
  PHOTO = "Photos",
  VIDEO = "Video & Audio",
  DOCUMENT = "Document",
  EMBED_LINK = "Embed link",
  UNKNOWN = "Unknown",
}

export enum EMediaTitle {
  MEDIA_GALLERY = "Media Gallery",
  THUMBNAIL = "THUMBNAIL",
}

export enum EMediaDefaultName {
  NO_NAME = "No name",
}

export enum MediaTypeEnum {
  IMAGE = "image",
  VIDEO = "video",
  AUDIO = "audio",
  DOCUMENT = "document",
  EMBED = "embed",
  OTHER = "other",
}
