export enum SidebarType {
  FIXED = 'fixed',
  CONDENSED = 'condensed',
  SCROLLABLE = 'scrollable'
}

export enum LeftSideBarIdEnum {
  // COMMON
  DASH_BOARD = 1,
  
  // OPERATION
  COLLECTION = 11,
  PROCESS = 12,
  USER = 13,
  GROUP = 14,
  ADMIN = 15,
  MESSAGE = 16,
  REPORT = 31,

  // SETTING
  DOCUMENT_TYPE = 21,
  TAG = 22,
  ICON_BUILDER = 23,

}